<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Survey</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <header>
        <div class="logo"></div>
        <nav>
            <ul>
                <li><a href="/">Home</a></li>
                <li><a href="/surveys">Surveys List</a></li>
                
            </ul>
        </nav>
    </header>
    <main>
        <h2>Edit Survey</h2>
        <form action="/editsurvey/<%= survey._id %>" method="POST">
            <label for="title">Title:</label>
            <input type="text" id="title" name="title" value="<%= survey.title %>" required>
            <br>
            <label for="question">Question:</label>
            <textarea id="question" name="question" required><%= survey.question %></textarea>
            <br>
            <label for="answerFormat">Answer Format:</label>
            <select id="answerFormat" name="answerFormat">
                <option value="text" <%= survey.answerFormat === 'text' ? 'selected' : '' %>>Text</option>
                <option value="multipleChoice" <%= survey.answerFormat === 'multipleChoice' ? 'selected' : '' %>>Multiple Choice</option>
            </select>
            <br>
            <button type="submit">Update</button>
            <button type="button" onclick="window.location.href='/surveys'">Cancel</button>
        </form>
    </main>
</body>
</html>
